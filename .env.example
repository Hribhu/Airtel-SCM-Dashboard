PORT=4000

# Comma-separated list of allowed frontend origins.
# Use "*" only for local testing — lock this down for production.
CORS_ORIGIN=https://airtel-scm-dashboard.vercel.app,http://localhost:5500
